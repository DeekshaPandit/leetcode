var maxSubArray = function(nums) {
    if(!nums) {
        return 0;
    }

    if(nums.length ==1) {
        return nums[0]
    }
    else {
        let sum =0;
        let subArr =[];
        for(let i=0; i<nums.length; i++) {
            sum = sum + nums[0]
            

        }
    }
};